/**
 *
 * @param ids S3 Bucket Ids
 */
export function classifyImages(bucketId: string) {
    /**
     * Go out to S3
     * Get images
     * Send to Rekognition
     * Get back classified images (and their bounding boxes)
     */
}
export function getOriginalImages(ids: string[]) {
    /**
     * Get original image for id in `ids` from S3
     */
}